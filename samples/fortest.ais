Script to test for()..next() loop
22/September/2001
Amit Singh

!include( stand.ail )

@cls()

@usr(final,n)
@usr(i,n)

@println(Initial value of control Variable 'i' = 0)
@input(Final Value of 'i':,final)
@println(Using step 2.5)


@for(i,0,@get(final),2.5)
	@println(in for i = @get(i,%.1))
@next()
