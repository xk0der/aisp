Testing !include() preprocessor directive
2/September/2001
Amit Singh

!include(stand.ail)
!include(testlib.ail)

@usr(str,s)
@usr(num,n)

@cls()
	@print(using \2print\9\0 to display this\n)

	@dis(Under Development!!\n)
	@dis(Enter a string :) @inp(str)
	@dis(Enter a number :) @inp(num)

	@println(string = @get(str))
	@println(number = @get(num))

@end()
